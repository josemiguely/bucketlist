<h1>Felicidades !</h1>

<p>Tu <%= link_to @bucket.title, bucket_url(@bucket) %> ha sido commpletado en la siguiente fecha: <%= @bucket.updated_at %>.</p>
<%= link_to "Unsubscribe", unsubscribe_url(token: params[:subscriber].generate_token_for(:unsubscribe)) %>