<%= debug @buckets %>
<h1>Your Buckets</h1>

<%= link_to "Crear un nuevo bucket", new_bucket_path %>

<div>
    <% @buckets.each do |bucket| %>
        <p><%= link_to bucket.title, bucket %></p>
    <% end %>
</div>
